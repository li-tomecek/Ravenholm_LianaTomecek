using UnityEngine;

public class TriggerPlateKey : MonoBehaviour
{
    //Because I can only add one tag and layer at a time, and these objects need to be grabbable and magnetic, 
    //this will just be an empty script that acts as a tag for now.
}
